<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hemisferio_derecho_app">

    <application
        android:allowBackup="true"
        android:icon="@drawable/principal"
        android:label="@string/app_name"
        android:roundIcon="@drawable/principal"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".Historia4" />
        <activity android:name=".Historia3" />
        <activity android:name=".Historia2" />
        <activity android:name=".Historia" />
        <activity android:name=".AdondeQuieresIr" />
        <activity android:name=".Registro" />
        <activity android:name=".Bienvenida" />
        <activity android:name=".InicioSesionOpciones" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>